// Temporary re-export shim: this module moved to `src/shared/user-message.ts`
// (it's shared logic — the CLI/TUI need it too). Delete this file once every
// importer below has switched over (plan `docs/plans/tagged-user-messages.md`
// task T5).
export * from "../../shared/user-message.ts";
